<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:VocableTrainer;assembly=VocableTrainer"
             x:Class="VocableTrainer.TrainingPageDetail"
             Title="Training">
    <Grid BindingContext="{x:Static local:App.Data}">
        <StackLayout IsVisible="{Binding NotPlaying}">
            <Picker Grid.Row="0"  x:Name="Language" Title="Language" 
                    ItemsSource="{Binding Languages}" 
                    ItemDisplayBinding="{Binding Name}"
                    SelectedItem="{Binding CurrentLanguage}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label VerticalTextAlignment="Center" Grid.Column="0" Text="Block size:" />
                <Label VerticalTextAlignment="Center" Grid.Column="1" Text="{Binding CurrentTraining.MostRecent}" />
                <Stepper Grid.Column="2" Minimum="0" Maximum="100" Increment="10"  Value="{Binding CurrentTraining.MostRecent}"/>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label VerticalTextAlignment="Center" Grid.Column="0" Text="Play answer" />
                <Switch x:Name="PlayAnswer" Grid.Column="1" IsToggled="{Binding CurrentTraining.PlayAnswer}"/>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label VerticalTextAlignment="Center" Grid.Column="0" Text="Auto play next" />
                <Switch x:Name="AutoPause" Grid.Column="1" IsToggled="{Binding CurrentTraining.AutoPlay}"/>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label VerticalTextAlignment="Center" Grid.Column="0" Text="One hand mode (swipe)" />
                <Switch x:Name="OneHandMode" Grid.Column="1" IsToggled="{Binding CurrentTraining.OneHandMode}"/>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label VerticalTextAlignment="Center" Grid.Column="0" Text="{Binding CurrentTraining.Pause}" />
                <Label VerticalTextAlignment="Center" Grid.Column="1" Text="sec. pause" />
                <Stepper Grid.Column="2" Minimum="3" Maximum="15" Increment="1"  Value="{Binding CurrentTraining.Pause}"/>
            </Grid>
            <Button x:Name="Restar" Text="Restart" Clicked="Restar_OnClicked"/>
            <Button x:Name="Resume" Text="Resume" Clicked="Resume_OnClicked"/>

        </StackLayout>
        <Grid IsVisible="{Binding Playing}">
            <Grid IsVisible="{Binding CurrentTraining.NotOneHandMode}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="Prev" Grid.Column="0" Text="Prev" FontSize="18" Clicked="Prev_OnClicked"/>
                    <Button x:Name="Pause"  Grid.Column="1" Text="Pause" FontSize="18" Clicked="Pause_OnClicked"/>
                    <Button x:Name="Next"  Grid.Column="2" Text="Next" FontSize="18" Clicked="Next_OnClicked"/>
                </Grid>
                <Button Grid.Row="1" FontSize="28" Text="{Binding CurrentVocable.Foreign}" x:Name="Foreign" Clicked="Foreign_OnClicked" />
                <Button Grid.Row="2" Text="{Binding CurrentVocable.Native}" FontSize="28" x:Name="Native" Clicked="Native_OnClicked"/>
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="Edit" Grid.Column="0" Text="Edit" FontSize="18" Clicked="Edit_OnClicked"/>
                </Grid>
            </Grid>
            <Grid IsVisible="{Binding CurrentTraining.OneHandMode}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Text="Swipe:  Up (Next)  Down (Prev)  Left (Pause)" />
                <BoxView Grid.Row="1" Color="#494949">
                    <BoxView.GestureRecognizers>
                        <SwipeGestureRecognizer Direction="Left" Swiped="SwipedLeft"/>
                        <SwipeGestureRecognizer Direction="Up" Swiped="SwipedUp"/>
                        <SwipeGestureRecognizer Direction="Down" Swiped="SwipedDown"/>
                    </BoxView.GestureRecognizers>
                </BoxView>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>